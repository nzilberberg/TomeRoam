# Secrets — never commit these (the app gets its Plex token at runtime, never from source)
*.token
*token*.txt
secrets.*
config.local.*
.env
*.key
*.pem

# Android build output (regenerated by android/build.ps1; APK ships as a Release)
android/build/

# Editor / OS cruft
*.swp
*~
.DS_Store
Thumbs.db
node_modules/

# Local project records (persona scheme) — kept off the public repo by choice.
# The decision log is a private working record for this machine, not a shipped artifact.
Claude/
