# Handoff — Zelda → Zelda, 2026-08-06

Written to be picked up **without the conversation that produced it.** Everything here was
reconciled against the repo at the moment of writing, not recalled.

---

## 1. State at handoff

| | |
|---|---|
| HEAD | `e9783f4` (local == remote after the final push) |
| Build | `2026-08-05.2` |
| Suite | **887 tests / 886 pass / 0 fail / 1 skip** (the skip is the device-only `KEEPER` cell) |
| Tree | clean |
| Campaigns | **14 of 14 COMPLETE** — every required gate has a filed, accepted verdict |
| Mutation registry | 152 entries |

⚠️ **`node` is NOT on PATH.** `NODE="$(git config --get tomeroam.node)"`.

---

## 2. Where we are in the actual spine — read this before planning anything

⛔ **There is no "phase 1–10". That framing does not exist in any plan** and cost this session three
failed attempts to answer "where are we". The real spine is:

**`Claude/Plans/PLAN-swipe-reveal.md` §7 STAGED EXTRACTION, at line 736. It is PLAIN TEXT, not
markdown** — every markdown-shaped grep returns nothing, which is why it stayed unfound. Ten stages:

| Stage | State |
|---|---|
| 1–5 | done |
| **6** | **done** — sliced 6a…6i, each with its own sub-plan and campaign manifest |
| **7** | **IN PLAN REVIEW, round 2 owed** — see §3 |
| 8 | not started — every new invariant active, mutation-verified, green |
| 9 | not started — device parity pass |
| 10 | not started — remove old diagnostics and simplify |

⚠️ **Two independent numbering systems both say "stage".** The de-clone workstream
(`PLAN-swipe-declone.md`) has its own Stage 1 / Stage 2 / subtraction, and the campaign manifests use
*those*. Reporting from manifests while the user asks about the spine is what caused the confusion.
Say which numbering you mean.

---

## 3. THE IMMEDIATE NEXT ACTION

**Dispatch Charpy for round 2 on `Claude/Plans/PLAN-swipe-stage7.md`.**

Status line reads *"TEMPER APPLIED (round 1) — returning to the plan reviewer."* Round 1 casebook:
`Claude/Charpy/PLAN-swipe-stage7-2026-08-06-r1.md`. The temper landed at `e9783f4`.

**Scope round 2 to the three structural resolutions — do not re-strike the scope.** Charpy
independently re-derived all ten inheritance rows in round 1 and every one held; re-doing that is
ceremony, and churn is the user's stated top concern.

What round 2 must judge:
- **F1** — §11 now declares three co-change classes (rotted `from` anchors 12/13; **`to`-side
  injections 9**, a class round 1 reported as 3; `ADAPTER_DECL`). The planner re-measured
  control-first and reproduced Charpy's numbers exactly, then found more.
- **F2** — the dead field is resolved by *building* the consumer (a guarded `PBDebug.log` in
  `returnLease`), not by citing a future one. Judge against EC §4.15.
- **F3** — R7's vacuous clause is retracted; the repaired predicate adds
  `typeof p.to === 'string' && !p.to.includes('Object.freeze(')`. Judge whether that discriminator
  actually isolates the wrapper-deletion mutant.

**After a FORGE: the adversary (Loki), which is the plan's own step 2.** The plan routes its exit
enumeration (`LEASEPAIRED`, I3) to Loki as an **open unknown** — Charpy ruled it *not answerable at
the plan-review seat*, because it asks for enumeration completeness and reading is all that seat does.
⛔ The strike must not be treated as confirmatory of the planner's reading.

---

## 4. Open items, none blocking stage 7

- **§14's deferred lifetime invariant** (`PLAN-swipe-declone-stage2-subtraction.md`) — the
  `Object.freeze` wrapper + wrapper-pin, two halves that must land together. **Now trigger-gated** by
  `MOVERLIFETIMETRIGGER` in `test/swipe-declone-stage2-subtraction.test.js`: it reddens when anything
  writes a mover *member* outside `toMover`. Verified red-on-counterexample, green-on-HEAD.
  ⚠️ The stage-7 transform satisfies §14's *prose* trigger (a new value threaded to the settle path)
  though not the mechanical clause, so stage 7 carries the freeze as a deliverable.
- **Two mechanisms still unbuilt**, both routed to the planner as tooling: (1) a check that every
  mutant a plan's `vitruvius-coverage` block declares exists in the registry; (2) a comparison of a
  registration's **stated** killers against its **measured** ones — the absence of (2) is what let
  `#13` sit reporting `caught` on cells that no longer existed.
- **`T-LP1` — letter pickers (A–Z strip).** User-reported, long-standing, explicitly **pre-existing
  and not caused by recent work**, deferred by the user until the campaign lands. Symptoms not yet
  derived; ask before routing.
- **De-clone device gate item 5** (a list > 600 items) — **DEFERRED**, the user has no such library.
  `tools/bench-virtual-swipe.mjs` narrows it on Blink but **does not close it**: it reads DOM
  geometry, not paint, and says nothing about iOS WebKit. Do not record it PASS.

---

## 5. Traps this session paid for — do not re-learn these

- ⛔ **`tools/source-gate-sweep.mjs` MUTATES `js/app.js`.** Run it directly; **never import it**.
  Same for never backgrounding `tools/mutation-sweep.mjs` — interrupted, it strands an applied mutant
  and greens the suite *because of it*.
- ⛔ **Cite mutants by NAME, never index.** The registry is 152 entries and every insertion shifts
  every later one. Three separate sessions were misled by stale indices.
- ⭐ **A mutation result stops being true when the suite changes.** Re-sweep against the final state.
- ⭐⭐ **An enumeration of blast radius has been incomplete NINE times in this campaign**, and every
  single instance was found by *executing or measuring* — never by a further reading. Treat any list,
  including a plan's own, as a hypothesis.
- ⭐⭐ **A checker that cannot be imported without side effects cannot be tested.** Three checkers this
  session ran their CLI on import; `node --test` reported `# tests 1 # pass 1` for a thirteen-test
  file and looked green. **The test COUNT was the only tell.**
- ⭐ **The plan gate is SLOW (~230 s), not hung.** Do not bound it at 60 s and call it broken — that
  error is recorded in memory `vitruvius-plan-gate-unreliable`, along with two invalid negative
  controls I built and believed.
- **Records: `git worktree` copies produce false grep hits.** A repo-wide search for a retired token
  hit a stale pre-declone worktree first and read as a surviving writer. All three worktrees are now
  reaped; `android/build/` is generated and gitignored.

---

## 6. Standing user rulings

- ⭐ **"You may adapt and learn by building gates. Beyond that use the scheme."** A gate closing a
  failure class just hit is Zelda's to build **directly**; every other craft routes to its seat.
  → memory `gates-are-mine-craft-is-the-schemes`.
- **Churn is the top concern** — "each edit is taking up to 10 hours". The legitimate lever is
  *ceremony* (fewer gates for a small change, stop re-gating frozen work), **never gate order**.
- **Do not stop after describing a dispatch.** Announcing agent work and ending the turn cost the
  user two hours once. The Stop hook now keys on the returning seat's own mandated *next owner*
  field rather than on phrasing.

---

## 7. Handoff packet (scheme shape)

- **Source artifact** — `Claude/Plans/PLAN-swipe-stage7.md`; round-1 casebook
  `Claude/Charpy/PLAN-swipe-stage7-2026-08-06-r1.md`.
- **Verdict / status** — TEMPER applied, round 2 owed.
- **Decisions made** — D1 settled (the release status ships and this stage builds its consumer);
  D2 follows as a consequence, not a free choice; D3 answered (freeze is runtime-neutral).
- **Open questions** — the exit enumeration (U1), owned by the adversary, not the reviewer.
- **Next owner** — **Charpy**, round 2, scoped to F1/F2/F3.
- **Required evidence / gates** — the six manifest gates for stage 7; the adversary before any build.
- **Records updated** — this file, the board, the decision log, and the memory index.
