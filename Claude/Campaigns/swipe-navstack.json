{
  "campaign": "swipe-navstack",
  "stage": "standalone",
  "status": "COMPLETE",
  "note": "Definition of done for the settle-window nav-stack fix — a STANDALONE slice, not part of the swipe/reveal spine's numbered stages, landing BEFORE stage 7. THE DEFECT (in shipped code): a nav tap during the roughly 340 ms settle window empties the stack the committing gesture is about to mutate. On the forward branch the commit pushes `undefined` and `runFinalize` throws at `dest.v`; on the back branch `navStack.pop()` is likewise unguarded and throws identically; and two further sequences corrupt the stack WITHOUT throwing, leaving the visible screen and `currentDesc()` disagreeing — so the throw is the loud instance of a quiet class. THE FIX: the commit's stack mutation becomes conditional on the stacks still describing the navigation the gesture planned, by OBJECT IDENTITY against the captured `cur.from` (a `.v` comparison misses it, because `navTo` REPLACES the top descriptor for a same-view nav), plus branch conjuncts that keep the pop total by construction; a stack-superseded settle reconciles `currentDesc()` and writes no scroll. ORDERING: this slice lands before stage 7 and shifts stage 7's five declared source ranges by 0, 0, 0, +11, +16 — a PER-RANGE correction, never a constant, measured by the plan reviewer. NO ADVERSARY GATE IS BOUND HERE: the plan's section 13 sequence names none, and that sequence is the artifact three review rounds forged. ⛔ ORACLE WARNING carried into every gate below: an oracle in this subsystem already proved nothing — the adversary's 'no @reveal report implies it threw' test fails because that line is emitted when the observation window CLOSES. Every cell's oracle must be proven able to FAIL, not merely cited. Checked by tools/campaign/stage-gate-check.mjs; every required gate carries a machine-readable verdict token. Globs are WILDCARDED so later review rounds stay visible — an exact filename makes every verdict after the first invisible to this manifest.",
  "gates": [
    { "gate": "plan-review",    "owner": "charpy",    "required": true, "verdictArtifactGlob": "Claude/Charpy/PLAN-swipe-navstack-settle-window-*.md", "acceptVerdict": ["FORGE"] },
    { "gate": "red-suite",      "owner": "curie",     "required": true, "verdictArtifactGlob": "Claude/Curie/*navstack*.md",                           "acceptVerdict": ["RED_SUITE_READY"] },
    { "gate": "build",          "owner": "brunel",    "required": true, "verdictArtifactGlob": "Claude/Brunel/*navstack*.md",                          "acceptVerdict": ["BUILD_GREEN"] },
    { "gate": "code-review",    "owner": "poirot",    "required": true, "verdictArtifactGlob": "Claude/Poirot/*navstack*.md",                          "acceptVerdict": ["PASS", "SHIP"] },
    { "gate": "coverage-audit", "owner": "mendeleev", "required": true, "verdictArtifactGlob": "Claude/Mendeleev/*navstack*.md",                       "acceptVerdict": ["ADEQUATE"] }
  ]
}
